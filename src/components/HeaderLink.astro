---
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'a'>;

const { href, class: className, ...props } = Astro.props;

const link = href as string;

const { pathname } = Astro.url;
const isActive = pathname === link;
---

<a
	href={href}
	class:list={[
		'border-b-4 border-transparent px-4 py-2 hover:bg-black hover:text-white dark:hover:bg-white dark:hover:text-black',
		className,
		isActive && '!border-green-500 font-extrabold'
	]}
	{...props}
>
	<slot />
</a>
